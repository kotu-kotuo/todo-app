= @board.title
= link_to "ホーム", root_path
= link_to "新しいタスク作成", new_board_task_path(board_id: @board.id)
- @tasks.each do |task|
  = link_to board_task_path(@board,task) do
    タスクタイトル
    = task.title
    タスクの内容
    = task.content
    タスクの期限
    = task.limit
